---
trigger: manual
---

Whenever possible, all app content must fit entirely within the initial visible
viewport (above the fold) without requiring the user to scroll. This responsive
constraint applies universally across all device sizes, including both PC and
mobile screens. A vertical scrollbar should only be introduced as a strict
fallback if the content absolutely cannot be contained within the available
screen height.
